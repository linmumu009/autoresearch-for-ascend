# v0.3.13 Update

Date: 2026-07-03

## Summary

Continued the MindSpeed-LLM 6-step learning-rate boundary search with
`LR=1.2e-4`, and tightened the runner's Ascend runtime library path handling.

## Result

| Candidate | LR | Steps | Raw HF Val Loss | Raw HF Val PPL | MindSpeed Valid Loss | Last Train Loss |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `lr_1p2em4_6step.env` | `1.2e-4` | 6 | `12.152827` | `189629.485` | `0.431454` | `0.644243` |

## Runtime Note

The first plain-runner launch after v0.3.12 could import `torch_npu` but failed
device discovery with `Megatron requires CUDA`. The runner now uses the same
minimal verified Ascend runtime `LD_LIBRARY_PATH` that made
`torch_npu.contrib.transfer_to_npu` report device availability correctly.

## Interpretation

`LR=1.2e-4` is the current best on the fixed raw-HF validation surface,
improving the runner baseline raw HF val_loss by `2.810139` and the base
Qwen3-0.6B raw HF val_loss by `2.824890`.

The 6-step search has not yet hit its raw-HF over-shoot point. The warning sign
is that MindSpeed-local valid and train losses keep rising, so the next
short-budget probe should be followed by a longer-budget confirmation once the
boundary is bracketed.
