# v0.3.14 Update

Date: 2026-07-03

## Summary

Continued the MindSpeed-LLM 6-step learning-rate boundary search with
`LR=1.3e-4`.

## Result

| Candidate | LR | Steps | Raw HF Val Loss | Raw HF Val PPL | MindSpeed Valid Loss | Last Train Loss |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `lr_1p3em4_6step.env` | `1.3e-4` | 6 | `12.037202` | `168923.703` | `0.446334` | `0.656360` |

## Interpretation

`LR=1.3e-4` is the current best on the fixed raw-HF validation surface,
improving the runner baseline raw HF val_loss by `2.925764` and the base
Qwen3-0.6B raw HF val_loss by `2.940515`.

The short-budget boundary is still not bracketed: raw HF val_loss improved by
`0.115625` over `1.2e-4`. The caution is unchanged: MindSpeed-local validation
and train losses are rising, so the high-LR region needs a longer-budget
confirmation once the 6-step over-shoot point is found.
