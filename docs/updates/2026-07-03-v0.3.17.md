# v0.3.17 Update

Date: 2026-07-03

## Summary

Continued the MindSpeed-LLM 6-step learning-rate boundary search with
`LR=1.6e-4`.

## Result

| Candidate | LR | Steps | Raw HF Val Loss | Raw HF Val PPL | MindSpeed Valid Loss | Last Train Loss |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `lr_1p6em4_6step.env` | `1.6e-4` | 6 | `11.906736` | `148262.033` | `0.489890` | `0.703591` |

## Interpretation

`LR=1.6e-4` is the current best on the fixed raw-HF validation surface,
improving the runner baseline raw HF val_loss by `3.056230` and the base
Qwen3-0.6B raw HF val_loss by `3.070981`.

The short-budget boundary is still not bracketed because raw HF val_loss
improved by `0.052228` over `1.5e-4`. Since MindSpeed-local losses are rising
steadily, the next probe should use a wider bracket, such as `2.0e-4`, instead
of continuing only `0.1e-4` increments.
